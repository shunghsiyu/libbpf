Enable testing of SL-16.1

From: Shung-Hsi Yu <shung-hsi.yu@suse.com>

Change vmtest-opensuse.yml so it support testing multiple different
kernel.

Signed-off-by: Shung-Hsi Yu <shung-hsi.yu@suse.com>
---
 .github/workflows/test.yml            |    9 +++++++--
 .github/workflows/vmtest-opensuse.yml |    9 +--------
 2 files changed, 8 insertions(+), 10 deletions(-)

diff --git a/.github/workflows/test.yml b/.github/workflows/test.yml
index 1754d3d..3339eaf 100644
--- a/.github/workflows/test.yml
+++ b/.github/workflows/test.yml
@@ -62,12 +62,17 @@ jobs:
       fail-fast: false
       matrix:
         include:
-          - kernel: 'LATEST'
+          - kernel: 'SL-16.0'
             runs_on: 'ubuntu-24.04'
             arch: 'x86_64'
             llvm-version: '18'
             pahole: 'master'
-    name: openSUSE SL-16.0 llvm-${{ matrix.llvm-version }}
+          - kernel: 'SL-16.1'
+            runs_on: 'ubuntu-24.04'
+            arch: 'x86_64'
+            llvm-version: '21'
+            pahole: 'master'
+    name: openSUSE ${{ matrix.kernel }} llvm-${{ matrix.llvm-version }}
     uses: ./.github/workflows/vmtest-opensuse.yml
     with:
       runs_on: ${{ matrix.runs_on }}
diff --git a/.github/workflows/vmtest-opensuse.yml b/.github/workflows/vmtest-opensuse.yml
index ad06519..e47b550 100644
--- a/.github/workflows/vmtest-opensuse.yml
+++ b/.github/workflows/vmtest-opensuse.yml
@@ -46,7 +46,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           repository: 'openSUSE/kernel'
-          ref: 'SL-16.0'
+          ref: ${{ inputs.kernel }}
           path: '${{ github.workspace }}/.kernel'
 
       - name: Patch kernel source
@@ -82,13 +82,6 @@ jobs:
           cp vmlinux ${{ github.workspace }}
           cd -
 
-      - name: Download prebuilt kernel
-        if: ${{ inputs.kernel != 'LATEST' }}
-        uses: libbpf/ci/download-vmlinux@v3
-        with:
-          kernel: ${{ inputs.kernel }}
-          arch: ${{ inputs.arch }}
-
       - name: Build selftests/bpf
         uses: libbpf/ci/build-selftests@v3
         env:
